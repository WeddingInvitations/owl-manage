const { Invoice } = require('./Invoice');
const { InvoiceItem } = require('./InvoiceItem');
const { Document } = require('./Document');
const { ProcessingResult } = require('./ProcessingResult');

module.exports = {
  Invoice,
  InvoiceItem,
  Document,
  ProcessingResult,
};
