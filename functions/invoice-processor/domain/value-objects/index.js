const { Money } = require('./Money');
const { DocumentId } = require('./DocumentId');
const { ProcessingStatus } = require('./ProcessingStatus');

module.exports = {
  Money,
  DocumentId,
  ProcessingStatus,
};
